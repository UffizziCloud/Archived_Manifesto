import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(() => ({
  link: {
    padding: '18px 50px',
    background: '#4B51D112',
    border: '1px solid #4B51D126',
    borderRadius: 4,
    display: 'inline-flex',
    marginTop: 20,
    cursor: 'pointer',
  },
}));

export default useStyles;
